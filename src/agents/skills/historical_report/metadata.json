{
  "name": "get_historical_report",
  "version": "1.0.0",
  "description": "Fetch a historical investment report (e.g., last week's WeeklyWorkflow) to compare current signals and justify strategy adjustments.",
  "category": "analysis",
  "tier": "smart",
  "input_schema": {
    "type": "object",
    "properties": {
      "report_type": {
        "type": "string",
        "description": "The type of report to fetch, e.g. 'WeeklyWorkflow' or 'DailyWorkflow'. Default is 'WeeklyWorkflow'."
      },
      "weeks_ago": {
        "type": "integer",
        "description": "How many weeks ago to fetch. Default is 1 (the most recent historical report before the current one)."
      }
    }
  },
  "output_schema": {
    "type": "object"
  },
  "platform": ["linux", "darwin"],
  "tags": ["strategy", "history", "reporting"]
}
