fastapi>=0.100.0
uvicorn>=0.20.0
requests-html>=0.10.0
beautifulsoup4>=4.12.0
pydantic>=2.0
lxml_html_clean>=0.1.0
# 2026-07-11: pin websockets — pyppeteer (via requests-html) resolves an old
# websockets (10.4) lacking websockets.server.ServerProtocol, which uvicorn's
# "auto" websocket protocol needs. Without this pin uvicorn crash-loops on
# startup (ImportError). Verified uvicorn 0.51.0 + websockets 13.1 + pyppeteer
# 0.0.25 resolve and import cleanly together.
websockets>=13,<14
